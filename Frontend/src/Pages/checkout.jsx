import { useSelector } from "react-redux";


const CheckOut=()=>{
    const data = useSelector(state=>state.Cart.items);
    const = totalAmount += key.price * key.qnty;
    {
        
    }
    
    return(
        <>
            <h3 style={{color:"green"}}>Total Amount : </h3>
        </>
    )
}
export default CheckOut